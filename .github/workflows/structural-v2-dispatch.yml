name: Structural v2 validation probe
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - run: echo safe
  reusable:
    needs: prepare
    uses: ./.github/workflows/reusable-advanced.yml
    strategy:
      matrix:
        flavor: [default]
