name: Structural v2 validation probe
on:
  workflow_dispatch:
permissions:
  contents: read
cache-mode: read
concurrency:
  group: structural-v2-fixture
  queue: single
jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - run: echo safe
